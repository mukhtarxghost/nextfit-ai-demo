[project]
name = "cloudflare-worker"
version = "0.1.0"
description = "Vantix NextFit AI Receptionist"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "pydantic",
    "httpx",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
